<?php
/**
 * Plugin Name: Claude Bridge
 * Description: Optional server-side deep layer for wp-claude-bridge. Exposes a one-call
 *              site context endpoint, read-only introspection, and bounded gap-filler
 *              primitives (dry_run by default). Auth via nonce or application password.
 * Version:     0.1.0
 *
 * STUB — endpoints to implement (see README):
 *   GET  context        site/plugins/woocommerce/post_types/taxonomies/acf/lms/rest_roots/capabilities
 *   GET  introspect/*   registered hooks, action scheduler jobs, logs, DB schema
 *   POST primitives/*   gap-fillers with no core REST equivalent (e.g. lms_enrollment_transfer)
 */
if ( ! defined( 'ABSPATH' ) ) { exit; }
// implementation pending
